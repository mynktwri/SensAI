How to use the NLP
Process.py is the only thing you are going to need.
In order to convert a text to a tree, import Process and
use Process.NLP(text).
In order to turn on the stanford server,
Go into the stanford server directory, and use this command:
java -mx4g -cp "*" edu.stanford.nlp.pipeline.StanfordCoreNLPServer -preload tokenize,ssplit,pos,lemma,ner,parse,mention,depparse,dcoref,ner,sentiment,natlog,openie
Copy paste is the best :3
